`ngModelAttrs` lets you add attributes to your form fields. They can be:
 
 * ng-directives (eg. ng-pattern, ng-minlength)
 * custom directives
 
 The title you give your `ngModelAttrs` can be used as its `validation.messages` name. 
